/**
 * ATOMS — The smallest, indivisible UI building blocks.
 *
 * Rules:
 * - No business logic
 * - No API calls
 * - Accept only primitive props (string, number, boolean, style, onPress)
 * - Must be fully reusable across the entire app
 *
 * Examples:
 * - Button, Text, Icon, Badge, Avatar, Spinner, Divider,
 *   Input, Chip, Tag, TextArea, Checkbox, Radio, Toggle
 */
