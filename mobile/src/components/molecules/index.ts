/**
 * MOLECULES — Combinations of 2+ atoms that form a distinct UI unit.
 *
 * Rules:
 * - Composed only of atoms (or other molecules)
 * - May have simple local state (e.g. toggle visibility)
 * - No direct API calls — receive data via props
 * - Should represent a single, focused function
 *
 * Examples:
 * - SearchBar (Input + Icon atom)
 * - SeatCard (Badge + Text + Icon atoms)
 * - TripCard (Avatar + Text + Badge atoms)
 * - FormField (Label + Input + ErrorText atoms)
 * - NotificationItem (Icon + Text + Timestamp atoms)
 * - MessageBubble (Avatar + Text + Timestamp atoms)
 */
